{
  "compilerOptions": {
    "target": "ESNext",                // Set the JavaScript version for emitted JavaScript and include compatible libs
    "module": "ESNext",                // Use ES modules
    "strict": false,                   // Typescript not required
    "esModuleInterop": true,           // Disables namespace imports (import * as foo from "foo") and enables CJS/AMD/UMD style imports (import foo from "foo")
    "skipLibCheck": true,              // Skip type checking of all declaration files (*.d.ts)
    "allowJs": true,                    // Allow JavaScript files to be compiled.
    "declaration": true,                // Generates corresponding '.d.ts' file for each TypeScript file.
    "declarationMap": true,             // Generates a sourcemap for each corresponding '.d.ts' file.
    "sourceMap": true,                  // Generates corresponding '.map' file for each compiled JavaScript file.
    "emitDeclarationOnly": false,       // Ensures that '.js' files are emitted alongside '.d.ts' file
    "outDir": "./dist"                 // Redirect output structure to the directory
  },
  "include": ["src/**/*"],            // Include all files in the src directory
  "exclude": ["node_modules"]         // Exclude the node_modules directory
}